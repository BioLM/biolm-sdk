name: Deploy Documentation

on:
  push:
    branches: [ main ]
    paths:
      - 'docs/**'
      - 'biolm/**'
      - 'Makefile'
      - 'scripts/generate_docs_manifest.py'
      - '.github/workflows/docs.yml'
  workflow_dispatch:  # Allow manual triggering

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y font-manager fonts-roboto fonts-noto || true

      - name: Install dependencies
        run: |
          python -m pip install --upgrade 'pip<25' setuptools typing-extensions
          pip install -r requirements_docs.txt
          pip install -e .

      - name: Build documentation (JSON export)
        env:
          # Prevent docs build from failing if an org/repo env sets BIOLM_THREADS too high.
          BIOLM_THREADS: "8"
        run: make docs-publish

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs/_build/publish

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
