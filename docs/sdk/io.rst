I/O (CSV, FASTA, JSON, PDB)
===========================

The SDK provides helpers for reading and writing CSV, FASTA, JSON, and PDB. See the :doc:`api-reference/index` for ``biolm.io`` and the :ref:`disk-output` section in :doc:`usage/usage` for writing results to disk.
