"""Optional plugin backends for BioLM (MLflow, and future integrations).

Plugin modules are not imported from the top-level ``biolm`` package. Install
extras as needed (e.g. ``pip install biolm-sdk[mlflow]``).
"""
